# Security Policy

## 报告安全问题

本项目涉及 Steam 账号凭证（API key、登录会话 cookie、代理凭据），安全问题优先于普通 bug。

**请不要**在公开 Issue 中描述漏洞细节。请发送邮件或私信维护者，包含：

- 问题类型与影响范围
- 复现步骤（或最小代码片段）
- 受影响的命令/代码位置（如 `src/steam_cli/auth.py`）

## 已知安全设计

- 高敏感凭证（Web API key、会话 cookie、代理 URL）存于操作系统 keyring；keyring 不可用时回退到 `~/.config/steam-cli/*.secret`（0600 权限）
- 写操作（激活、愿望单、评价、邀请链接）需要登录会话，且不触达任何资金/交易功能
- 本地审计日志仅本地保存，不上传

## 安全声明

本工具仅供个人账号管理，网页会话类功能（CDK 激活、评价发布等）属于 Steam ToS 灰色地带，风险由使用者自行承担；请勿用于商业自动化。
